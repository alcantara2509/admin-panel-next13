import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1a1a1a",
    },
    secondary: {
      main: "#4affba",
    },
    lightGray: "#4d4d4d",
  },
});

export default theme;
